import logo from "../image/icon.png";

function Effect() {
  return (
    <div className="effect">
      {/* CSS로 파동 효과 넣기 */}
      <img src={logo} alt="logo" />
    </div>
  );
}

export default Effect;
